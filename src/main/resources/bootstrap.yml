server:
  port: 9090

spring:
  application:
    name: streamlink
  cloud:
    config:
      uri: http://localhost:1212

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}